<%= render "users/banner" %>

  <ul>
     <% @professionals.each do |professional| %>
      <li>
        <%= link_to professional.first_name, user_path(professional) %>
        <% if professional.category.nil? %>
        Categoria não definida
        <% else %>
          <%= professional.category.name %>
        <% end %>
      </li>
     <% end %>
   </ul>
